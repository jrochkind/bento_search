module BentoSearch
  class Engine < ::Rails::Engine
    #isolate_namespace BentoSearch

    # initializer "bento_search.assets.precompile" do |app|
    #   app.config.assets.precompile += %w( bento_search/large_loader.gif )
    # end
  end
end
